import { StyleSheet } from "react-native";

export const stylesGeral = StyleSheet.create({
    textInput : {
        height: 40,
        borderColor: "#ccc",
        borderWidth: 1,
        borderRadius: 5,
        paddingHorizontal: 100,
        marginBottom: 15,
    },

});